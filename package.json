{
    "name": "spotify-user-info",
    "private": true,
    "scripts": {
        "build": "npm --prefix client ci --include=dev && npm --prefix client run build && npm --prefix server ci",
        "start": "node server/src/index.js"
    }
}
